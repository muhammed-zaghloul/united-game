# Example environment variables
# Copy this file to .env.local (or .env) and fill in your values.
# Do NOT commit your local env files to source control. Add them to .gitignore.

# API key used by server-side code or CI. Add the real key to GitHub Secrets for CI.
GEMINI_API_KEY=your_gemini_api_key_here
